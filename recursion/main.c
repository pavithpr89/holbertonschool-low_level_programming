#include "main.h"
#include <stdio.h>

/**
 * main - check the code
 *
 * Return: Always 0.
 */
int main(void)
{
	int len;

	len = _strlen_recursion("Pav is awesome");
	printf("Length: %d\n", len);
	return (0);
}
